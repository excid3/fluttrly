<?xml version="1.0" encoding="UTF-8"?>
<Response>
    <% if @success %>
      <Sms>Successfully added to <%= request.env["HTTP_HOST"] %>/<%= @name %></Sms>
    <% else %>
      <Sms>Fluttrly: Invalid format. Use the following example "LIST: TASK HERE"</Sms>
    <%- end -%>
</Response>

