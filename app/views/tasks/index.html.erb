  <h2>
    <%= link_to (image_tag "unlocked.png", :class => :right), "/lock" %>
    <%= params[:name] %>
  </h2>

  <%= render 'form' %>

  <p id="errors"><%= params[:errors] %></p>
  <p id="notice"><%= notice %></p>

  <ul id="tasks">
    <%= render :partial => 'task', :collection => @tasks %>
  </ul>
