<!DOCTYPE html>
<html>
<head>
  <title>
    <%- if params[:name] -%>
      <%= params[:name] %> (<%= @count %>) on Fluttrly
    <%- else -%>
      Fluttrly - Simple Online Todo Lists
    <%- end -%>
  </title>

  <link rel="shortcut icon" href="/images/favicon.ico">

  <%= stylesheet_link_tag "scaffold.css" %>

  <meta name="viewport" content="initial-scale=1.0, user-scalable=no">

  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<div id="wrapper">
  <div id="header">

    <a href="/"><%= image_tag "fluttrly.png" %></a>

    <ul id="nav">
      <% if not user_signed_in? %>
        <li><%= link_to "Sign Up", new_user_registration_url %> or <%= link_to "Login", new_user_session_url %></li>

      <% else %>
        <li><%= link_to "Your Lists" %>
          <ul>
          <% if current_user.lists.empty? %>
            <li>You have not locked any lists</li>
          <% else %>
            <% current_user.lists.each do |list| %>
              <li><%= link_to list.name, "/#{list.name}" %></li>
            <% end %>
          <% end %>
          </ul>
        </li>
        
        <li><%= link_to "Account", edit_user_registration_url %></li>
        <li><%= link_to "Logout", destroy_user_session_url %></li>
      <% end %>
    </ul>

  </div>
  <% if notice %><p class="notice"><%= notice %></p><% end %>
  <% if alert %><p class="alert"><%= alert %></p><% end %>

<%= yield %>

</div>

<div id="footer">
  Lovingly created by <a href="http://excid3.com">Chris Oliver</a>
</div>

<script type="text/javascript">

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-8399719-10']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();

</script>

</body>
</html>
